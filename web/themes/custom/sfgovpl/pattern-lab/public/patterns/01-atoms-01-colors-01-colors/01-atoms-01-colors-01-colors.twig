<ul class="sg-colors">
  {% for colorset in colorset %}
    <li>
      <span class="sg-swatch" style="background: {{ colorset.color }};"></span>
      <span class="sg-label">{{ colorset.color }}<br/>${{ colorset.label }}</span>
    </li>
  {% endfor %}
</ul>
