{% set url = path('entity.node.canonical', {'node': elements['#node'].id()  }) %}
{% set description = node.get('field_description').getValue()[0]['value'] %}

{{ devel_breakpoint() }}

{{ kint(node.get('field_departments').getValue()) }}

{% set relatedDepts = node.get('field_departments').getValue() %}
{% set relatedDept = null %}

{% set break = false %}
{% for i, value in relatedDepts if not break %}
  {% if node.get('field_departments')[i].entity %}
    {# this is an entity<br/> #}
    {% set relatedDept = node.get('field_departments')[i].entity.getTitle() %}
    {% set break = true %}
  {% endif %}
{% endfor %}

{% if view.id == 'services' %}
{%  set relatedDept = null %}
{% endif %}
{% include "@molecules/08-search-results/01-transaction-search.twig" with {
  "url": url,
  "title": elements['#node'].get('title').getString(),
  "body": description,
  "relatedDept": relatedDept
} %}