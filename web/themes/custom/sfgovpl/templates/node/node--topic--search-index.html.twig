{% set type = node.type.entity.label %} {# content type #}

{% include '@theme/search-result-topic.twig' with {
  content_type: type,
  url: url,
  title: node.label,
  body: node.field_description.value
} %}
