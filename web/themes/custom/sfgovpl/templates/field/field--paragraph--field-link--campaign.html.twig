{#
/**
 * @file
 * Removes field wrapper markup for fields on 'campaign' paragraph.
#}
{% for item in items %}
  {% include "@sfgov-design-system/button/link.twig" with {
    "href": item.content['#url'],
    "text": item.content['#title'],
    "target": item.content['#options'].attributes.target,
    "classes": item.content['#options'].attributes.class,
  } %}
{% endfor %}
