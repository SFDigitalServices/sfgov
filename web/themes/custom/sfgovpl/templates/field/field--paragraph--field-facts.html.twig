<div class="campaign-facts">
  <div class="row">
  {% for item in items %}
    <div class="fact-item">{{ item.content }}</div>
    {% if loop.index is divisible by(3) %}
      </div>
      <div class="row">
    {% endif %}
  {% endfor %}
  </div>
</div>