<nav class="sfgov-main-navigation">
  <ul class="menu">
    {% for item in items %}
      <li{{ item.attributes.addClass(classes) }}>
        {{ link(item.title, item.url) }}
      </li>
    {% endfor %}
  </ul>
</nav>
