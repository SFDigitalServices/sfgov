{
  "scripts": {
    "build": "npm run build-js",
    "build-css": "postcss --verbose -d dist/css 'css/src/*.css'",
    "build-js": "babel -d dist/js 'js/src/*.js'",
    "lint-js": "eslint .",
    "watch-js": "npm run build-js -- --watch",
    "watch": "npm run watch-js"
  },
  "devDependencies": {
    "@csstools/postcss-sass": "^5.0.1",
    "postcss": "^8.4.18",
    "postcss-cli": "^10.0.0",
    "postcss-import": "^15.0.0",
    "postcss-scss": "^4.0.5"
  }
}
