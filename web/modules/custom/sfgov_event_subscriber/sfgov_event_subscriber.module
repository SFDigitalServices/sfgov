<?php

function sfgov_event_subscriber_views_post_build(&$view) {
  // Testing to see when this passes to event subscriber.
  $view->args[1] = 160;
}

function sfgov_event_subscriber_views_pre_view($view, $display_id, array &$args) {
  // Testing to see when this passes to event subscriber.
  $args[1] = 160;
}